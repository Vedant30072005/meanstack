#include<iostream>
using namespace std;
int main()
{
    //pointer is a variable that stores the address of another var
    //int pointer cn store the address of int variable only not float or any other
    //same goes for float and other types
    //int *a; and int* a; both can be written
    int a=5;
    float b=2.1;
    int *c=&a; //pointer-to-int
    float *d=&b;//pointer - to - float
    int arr[10];
}
